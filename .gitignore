# build output
dist/
.astro/

# dependencies
node_modules/

# original card scans (~306MB); the optimised derivatives are committed.
# Kept outside public/ so Astro does not copy them into dist/ on build.
assets/card-scans/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# environment
.env
.env.production

# macOS
.DS_Store

# playwright mcp scratch output
.playwright-mcp/

# lightfactory
.light
# yaks task graph
.yaks/

# agent harness: the committed parts are .claude/settings.json and
# .claude/hooks/. Worktrees are scratch checkouts and personal settings
# overrides are per-machine, so neither belongs in the repo.
.claude/worktrees/
.claude/settings.local.json
